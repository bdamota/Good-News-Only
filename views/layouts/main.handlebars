<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Good News Only.</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="wrapper">
   <header>
    <h1>
      <a href="/">Good News Only.</a>
    </h1> 
    <nav>
      {{#if loggedIn}}
      <a class="home" href="/">Homepage</a>
      <a class="dash" href="/dashboard">Dashboard</a>
      <button id="logout" class="btn-no-style">Logout</button>
      {{else}}
      <a class="home" href="/">Homepage</a>
      <a class="dash" href="/login">Dashboard</a>
      <a href="/login">Login</a>
      {{/if}}
  </nav>
  </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
     Good News Only. &copy 2020
    </footer>
  </div>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>